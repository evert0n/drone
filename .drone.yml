pipeline:
  test:
    image: node:${NODE_VERSION}
    commands:
      - npm install --silent
      - npm test
    environment:
      - NODE_ENV=test
  slack:
    image: plugins/slack
    environment:
      - SLACK_WEBHOOK=${ZENBOT_WEBHOOK_URL}
    #webhook: ${ZENBOT_WEBHOOK_URL}
    channel: zenbot
    when:
      status: [ success, failure ]

matrix:
  NODE_VERSION:
    - 6-alpine
    - 8-alpine
    - 9-alpine
    - alpine
    - latest
